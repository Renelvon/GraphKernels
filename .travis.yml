dist: bionic

language: python

os: linux

python: 3.6

virtualenv:
    system_site_packages: true

before_install:
    - sudo apt-get install -yq libeigen3-dev python3-igraph python3-numpy python3-pkgconfig swig

install:
    - cd src
    - python3 setup.py install
    - cd graphkernels
    - python3 setup.py install

script:
    - cd ../../Tutorial
    - python3 demo_mutag.py
    # The following requires sklearn package
    #- python3 demo_grid_search.py
